.env
*.log
# Only the compiled binary at the repo root — NOT the cmd/emergency-callback/ source dir.
/emergency-callback
dist/
.idea/
.vscode/
static/

# Docs build artifacts
site/
.docs-venv/

# Installer output — contains secrets, never commit
INSTALL_CREDENTIALS.txt
freepbx-bundle/
.env.bak.*
